package fcoin

import "ccxt/config"

const (
	Name = "fcoin"

	FCoinErrorLimit = config.DEFAULT

	FCoinBufferSize = config.DEFAULT_BUFFER_SIZE

	FCoinWsUrl = "wss://api.fcoin.com/v2/ws"

	FCoinSymbol = "https://api.fcoin.com/v2/public/symbols"
)

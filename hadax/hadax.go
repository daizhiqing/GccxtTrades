package hadax

import "ccxt/config"

const (
	HadaxErrorLimit = config.DEFAULT

	HadaxBufferSize = config.DEFAULT_BUFFER_SIZE

	HadaxSymbol = "https://api.hadax.com/v1/hadax/common/symbols"

	HadaxWsUrl = "wss://api.hadax.com/ws"
)
